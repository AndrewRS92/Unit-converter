export default function ConvertedUnitsTable()
{
    // Pendiente de hacer la tabla y poder iterar el local storage para mostrar los datos guardados y poner botón de eliminar.
    return (
        <div>
            <h3>{localStorage.length} El dato guardado debería ir aquí</h3>
        </div>
        )
} 